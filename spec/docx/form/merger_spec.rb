require 'spec_helper'

describe Docx::Form::Merger do
  it 'has a version number' do
    expect(Docx::Form::Merger::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
